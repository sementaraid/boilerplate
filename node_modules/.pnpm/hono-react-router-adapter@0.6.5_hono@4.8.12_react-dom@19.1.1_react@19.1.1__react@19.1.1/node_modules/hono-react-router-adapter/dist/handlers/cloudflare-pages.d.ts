import * as hono_cloudflare_pages from 'hono/cloudflare-pages';
import { Hono } from 'hono';
import { GetLoadContext } from '../react-router.js';
import 'react-router';

type Options = {
    getLoadContext: GetLoadContext;
};
declare const handler: (serverBuild: any, userApp?: Hono<any, any, any>, options?: Options) => (context: hono_cloudflare_pages.EventContext<unknown, any, Record<string, unknown>>) => Response | Promise<Response>;

export { handler as default, handler };
