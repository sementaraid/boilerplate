import * as hono_types from 'hono/types';
import { Hono } from 'hono';
import { GetLoadContext } from './react-router.js';
import 'react-router';

type Options = {
    getLoadContext: GetLoadContext;
};
declare const handle: (userApp?: Hono, options?: Options) => Hono<hono_types.BlankEnv, hono_types.BlankSchema, "/">;

export { handle as default, handle };
